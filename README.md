# to-do-list
A web application for a To Do List to practice Python/Django and TDD

## Git hooks

Enable the repository hooks with:

```sh
git config core.hooksPath .githooks
```

The `commit-msg` hook rejects commit messages that do not follow Conventional Commits.
